// Mock Firebase API for in-memory storage
// No Firebase configuration required!

export const database = null; // Not used in local mode

// Mock ref, onValue, off - not used but kept for compatibility
export const ref = () => null;
export const onValue = () => null;
export const off = () => null;

